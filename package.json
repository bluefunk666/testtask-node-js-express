{
  "type": "module",
  "scripts": {
    "start": "npx sequelize-cli db:migrate --config src/config/config.json --migrations-path src/migrations && npx sequelize-cli db:seed:all --config src/config/config.json --seeders-path src/seeders && node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "sequelize": "^6.34.0",
    "sequelize-cli": "^6.6.2"
  }
}
